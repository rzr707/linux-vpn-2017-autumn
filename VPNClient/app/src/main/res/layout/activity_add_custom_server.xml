<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <LinearLayout android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="3mm">
        <TextView style="@style/item" android:text="@string/serv_name"/>
        <EditText style="@style/item" android:id="@+id/serv_name"/>
        <TextView style="@style/item" android:text="@string/address"/>
        <EditText style="@style/item" android:id="@+id/address"/>
        <TextView style="@style/item" android:text="@string/port"/>
        <EditText style="@style/item" android:id="@+id/port" android:inputType="number"/>

        <Button style="@style/item" android:id="@+id/connect" android:text="Update Spinner"/>
        <Button style="@style/item" android:id="@+id/disconnect" android:text="Cancel"/>
    </LinearLayout>
</ScrollView>